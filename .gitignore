# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
web-build/
.expo/
*.tsbuildinfo

# Expo generated types
expo-env.d.ts
public/canvaskit.wasm

# Test / coverage
coverage/
.vitest/
playwright-report/
test-results/

# Env / secrets (never commit API keys — see CLAUDE.md guardrails)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.local

# Claude Code local overrides (per-machine, not shared)
.claude/settings.local.json
